# Mock lecture for CHE358 (UoA ChME)
## Compilation
1. Requirements:
- `make`
- `python` with libraries `numpy`, `matplotlib`, `scikit-learn`
- `ffmpeg`
- `latex` engine (`TexLive >= 2022`)
2. Compilation
```
make all
```
## Running individual codes
```
cd scripts
python <example_name>.py
```
